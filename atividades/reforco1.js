  // colocando dentro da variavel apenas os numeros que são maiores que 0
/*
let vet = [1,-2, 3, -4, 5]
let numeros = []

for(let i = 0; i < vet.length; i++) {
    if(vet[i] > 0) {
        numeros.push(vet[i])
    }
}
console.log(numeros) */


// fazer a media dos preços

/*let precos = [1.99, 4.99, 7.99, 10.0, 15.0]
let soma = 0;
let media;

for(let i = 0; i < precos.length; i++) {
    soma = soma + precos[i]
}
media = soma / precos.length
    
    console.log(media)*/

// atividade reforço 
/*
let nome = 'maquina'
let valor = 2500.00
let quantidade = 3
console.log('a ' + nome + ' custa ' + valor + ' e eu comprei ' + quantidade)*/

// modificar idade 
/*let idade = 18
let mensagem
    if(idade >= 16) {
    console.log('voçê pode votar')
    } else {
    console.log('voçê não pode votar');
    }*/

    // preco maximo e minino
    /*let precos = [1.99, 4.99, 7.99, 10.0, 15.0]
    let maximo = precos[0]
    let minimo = precos[0]

    for (let i = 0; i < precos.length; i++) {
        if(precos[i] > maximo) {
            maximo = precos[i]
        } 
        else if(precos[i] < minimo) {
            minimo = precos[i]
            
        }
    }

    console.log('o preco minimo é' + minimo);
    console.log('o preco maximo é' + maximo);*/